[workspace]
resolver = "3"
members = ["debug", "packer", "proc_macros", "common", "stub"]

[workspace.dependencies]
debug = { path = "debug" }
proc_macros = { path = "proc_macros" }
common = { path = "common" }

[profile.release]
debug = "none"
strip = "symbols"
panic = "abort"
